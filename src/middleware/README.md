# Middleware Extension Point

Use this folder for future route guards, auth checks, request policies, and security middleware.

The current app has no fake authentication. Public routes remain public until a real auth provider and secure token strategy are selected.
