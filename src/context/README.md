# Context Extension Point

Use this folder for future app-wide providers that are genuinely shared across routes, such as authentication, cart state, preferences, or locale.

Prefer colocated state for page-only interactions.
